name: Deploy Astro Site to Pages

on:
  push:
    branches: [main]
    paths:
      - "JsPsych/landing-page/**"

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: "JsPsych/landing-page/package-lock.json"

      - name: Install dependencies
        run: cd JsPsych/landing-page && npm ci

      - name: Build
        run: cd JsPsych/landing-page && npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: JsPsych/landing-page/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
