var bloque_infinito = {
  timeline: [
    {
      type: jsPsychHtmlButtonResponse,
      stimulus: () => {
        return 'Haz clic en "Continuar" hasta que la condición se cumpla.';
      },
      choices: ['Continuar']
    }
  ],

  loop_function: function(data) {
    // 👇 Aquí defines la condición de salida
    // Por ejemplo, detener cuando se haya presionado el botón 5 veces
    let total_trials = jsPsych.data.get().count();

    if (total_trials < 5) {
      return true;  // 🔁 sigue repitiendo
    } else {
      return false; // 🛑 termina el bucle
    }
  }
};

jsPsych.run([bloque_infinito]);