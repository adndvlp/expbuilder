/**
 * @fileoverview Code generation service for experiments
 * This is a placeholder - actual code generation happens in the frontend
 * and is sent to the backend during publish
 */

/**
 * Placeholder function - not used
 * The public code is now generated in the frontend and sent with the publish request
 */
export async function generatePublicExperimentCode() {
  throw new Error(
    "Public code should be generated in frontend and sent with publish request",
  );
}
